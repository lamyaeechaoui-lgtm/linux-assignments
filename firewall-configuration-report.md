1. Introduction

This report describes the firewall configuration created to protect the server.

The firewall:

Loads automatically at server startup

Allows only required services:

OpenSSH (port 22)

HTTP (port 80)

HTTPS (port 443)

